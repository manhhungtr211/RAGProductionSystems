# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
*.egg-info/

# Environments
.env

# Bỏ qua các file key và chứng chỉ bảo mật
docker/volume/cache/*.key
docker/volume/cache/*.pem
docker/volume/cache/*.crt

# Bỏ qua file nháp cá nhân
bug.text
src/redis-test.py
CLAUDE.md

# --- PHẦN ĐÃ SỬA ĐỂ CHO PHÉP ĐẨY FAISS ---

# Chỉ bỏ qua file raw PDF và file nén, KHÔNG bỏ qua thư mục vector
data/raw/
*.zip
*.pdf

# Bỏ qua bộ nhớ đệm của Docker nhưng KHÔNG cấm file .faiss/.pickle
docker/volume/cache/